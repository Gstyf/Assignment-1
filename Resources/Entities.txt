entity { flags : movable ; char : p ; texture : player;}
    
entity { char: # ; flags: immovable; texture : wall; }

entity { flags : movable ; char : b ; texture : box; }

entity { flags : immovable ; char : s ; texture : switch ; }